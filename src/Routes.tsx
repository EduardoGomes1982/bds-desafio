import NavBar from 'components/NavBar';
import { BrowserRouter } from 'react-router-dom';

const Routes = () => (
    <BrowserRouter>
        <NavBar />
        <h1>Algo aqui</h1>
    </BrowserRouter>
);

export default Routes;